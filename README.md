# babel-preset-optimize

A Babel preset that converts transpiled ES5 code to ES2017.
It can remove most polyfills, even when they're inlined.

- [ ] Add missing globals
  - `Object.create`
  - `JSON.*`
